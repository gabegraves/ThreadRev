# Status board

One line per lane. Update yours after every push. Read all four before you start. Times are EDT, 2026-09-12. Deadline 5:00 PM.

| Lane | Owner | Last pushed | Editing now | Blocked on |
|---|---|---|---|---|
| Backend / reviewer | Gabe | 1:50 G2 wired: `npm run replay:record -- <fixture> --model [--runs N]` runs the real reviewer agent through the harness; exits 3 while `OPENAI_API_KEY` is empty. `npm run verify` green (56 + 45 + 34 tests, 10 checker tests) | fixes from eval results; live Slack smoke test when the key lands | `OPENAI_API_KEY` |
| Web console | Soham (28gugales-dev) | 3:05 `481f355` on branch `web-console` (merged main at bfc634e), live at https://threadrev-web.vercel.app (CLI deploy from the branch, root `apps/web`): Civic 1:1 console; distilled pages (one question each, nothing shown twice); findings + thread as Civic AG Grid; evidence graph = d3 port of soham-research explorer with live physics; workspace, documents, runs with `?id=`/`?ts=` deep links; `npx tsc --noEmit` in apps/web passes | Antigravity delegation setup; PR `web-console` → `main` | Gabe: review/merge `web-console`; chat needs OPENAI_API_KEY on Vercel |
| Demo and submission | Codex | Isolated rehearsal on `codex/actual-ui-demo`, based on `origin/web-console` 481f355: /demo reuses actual findings, thread explorer, D3 graph, and approval event feed; localhost:3120. Before/after snapshots use Scenario A fixture events. | Live UI reads main checkout evidence/log.jsonl through ignored local env; missing log is visibly sample data. | Target Slack workspace T0C1GD3C8FK needs browser sign-in; live delivery not verified here. |
| Slack environment and credentials | computer-use agent (handoff `research/handoff-slack-lane.md`) | 1:20 handoff written; `channel:status` at 1:09 returned `CLI_CHANNELS_PROJECT_NOT_SELECTED`; `OPENAI_API_KEY`, `CHANNEL_CODE`, `INTELLIGENCE_API_KEY` still blank | A1 login + project select | human: CopilotKit login, Slack workspace admin, OpenAI key (Q1 to Q3 in the handoff) |
| Eval / red team | fill in (ML teammate) | nothing yet; human handoff `research/handoff-eval-lane.md`, agent brief `research/agent-briefs/w5-eval-mechanics.md` | fill in | backend lane: run records (G1) by 1:00 PM; Slack lane: `OPENAI_API_KEY` for model mode |

## Decisions and announcements

- 4:20 Why Card 1 did not post live at 19:03Z (backend lane): the model passed `requirements_revision` = the fixture ts of the seed message it saw through `search_workspace`, the live thread's revision was the seed's occurredAt, `publish_result` compared strings and refused the card as stale, and the re-read plus re-run exhausted the 10-step budget. Fixed in `21b6295`: `run_check` records the thread revision at run time and cards bind to that (the model's string survives when it names the same second), and the reviewer gets 24 steps. Channel tests 48 green. The live process on :54266 restarted with the fix; session 9-12-26-hackathon-70 is re-running A4 in the existing test thread. Chrome on this machine is signed into `kestrelmotors.slack.com`, not `kestrel-motors.slack.com`, so browser posting from here needs a human sign-in first.

- 4:00 Slack lane, checked by Gabe's agent: no Slack app exists. `.copilotkit/channels.json` holds only the two env var NAMES, `channel:status` says adapter slack `absent`, provider `not_attached`, and `dev:slack` exits with `setup_required`. Manifest regenerated at `.copilotkit/artifacts/threadrev/slack-app-manifest.yaml` (bot display name Rev, managed request URLs). Human steps: api.slack.com/apps → Create New App → From a manifest → workspace Kestrel Motors → paste the YAML → Install to Workspace → copy Bot User OAuth Token (`xoxb-`) and Signing Secret into `.env` as `INTELLIGENCE_CHANNEL_THREADREV_SLACK_BOT_TOKEN` and `INTELLIGENCE_CHANNEL_THREADREV_SLACK_SIGNING_SECRET` (placeholders appended). Then the agent runs `copilotkit channels add threadrev --adapter slack --json`, `channel:status`, `dev:slack`, and the A4 script. Channel code stays `threadrev`; the bot's Slack name is Rev.

- 3:50 First live model runs are green (backend lane). Harness fix: the SDK test gateway gave a delivery 1 s to close, fine for the scripted reviewer, fatal for a real model; `ManagedGateway` now takes `deliverTimeoutMs`, 5 min in model mode. Tool fixes after two identical failures on both models: `publish_result` drops an empty `sha256` on message sources instead of rejecting the card, `run_check` says the checker is always `rc`, and the prompt says both plus "if publish_result rejects a field, fix it and call again". Results for Scenario A, one run each: `gpt-5.6-sol` 45 s, 12 tool calls, Card 1 as scripted (680 vs 750 uF, 2.2077 / 2.435 / 6.4933 s, question to Dara) plus the section 4 proposal (`evals/records/model/scenario-a.1.json`). `gpt-5.6-luna` 26 s, 9 calls, Card 1 also cites the purchasing 820 uF snubber message from `search_workspace` and asks 680 vs 820, which pre-empts the Dara correction beat (`scenario-a.2.json`). Keep `MODEL=gpt-5.6-sol` for the video. Both names confirmed on the account's model list (also `gpt-5.6-terra`). The key is in `.env`; rotate it after the event, it was echoed once into an agent transcript.

- 3:25 Branch survey (Gabe). `origin/web-console` tip `bfc634e` merged main back in, commits the thread grid, adds the vis-network graph explorer and Vercel timestamp fix, and is live at https://threadrev-web.vercel.app. Test merge into main in a scratch worktree: clean, verify 56 + 47 + 34 + 12, web build green; ready to merge on request. `origin/sricharan/work` (43 ahead, 42 behind, base `b62bd38`) carries `apps/pet`, an Electron always-on-top desktop character that sits above the Slack desktop app and polls `http://localhost:3000/api/pet/findings`, which no server on any branch implements; it also changes `apps/channel` (in-thread mute/resume/explain in `control.ts`, boot-time model check, docx size guard) and conflicts with main in seven files. Do not merge wholesale; cherry-pick after the video. `origin/zamp-icons`: role icons, web only. `OPENAI_API_KEY` is still empty in `.env` (mtime 13:33); the live model pass has not run.

- 3:10 The first console's bottom-right pill and drawer (Issues, Diffs, Evidence, Chat) are mounted in the Civic layout on every page (`apps/web/src/components/shell/console-overlay.tsx`, `7991928`, `e7f5ae3`). They follow the scenario switcher and register the scenario as agent context for the Chat tab. Checked in Chrome on :3100: pill renders, drawer opens full height on the overview and graph pages, Issues shows the live and stale cards, Chat renders its input, the activity feed shows edit proposed, decided, applied. The Chat tab talks to the inherited starter agent, not Rev, and needs `OPENAI_API_KEY` like everything else. One pre-existing console error on every page load: CopilotKit's runtime-info probe tries the single-route envelope first and gets a 404 before falling back; not caused by the overlay, not chased.

- 2:55 Console demo data now carries the approval beat: `build-demo-data.py` adds `edit_proposed` after Card 1 (section 4, `t = 6.91 s` → `t = 6.493 s`, proposal `edt-a-r2-001`) and `edit_decided` (approved, Juno Marsh) plus `edit_applied` after Card 2. The applied sha256 is computed at build time by running `checkers/apply_docx_edit.py` into a temp dir, not typed. All 80 demo events validate against the evidence contract. `workspace.ts` regenerated with the `@Rev` rename. Preview rebuilt and running on :3100 (`next start`, log in the session scratchpad); `/api/evidence` 200, sample mode. Path B recording can show beat 6 from the console's scenario-a timeline.

- 2:40 `web-console` merged to main at `65dd51d` (branch commit `1e697be`, not the wip tip `39267ec`, whose thread-grid imports are uncommitted). Root page is now Soham's Civic console; 15 routes build. Three `propose_edit` event kinds added to the console's kind labels. `packages/context-store` (embeddings, MMR) stays on the branch, unmerged: nothing imports it and it contradicts README "Why search by identifier, not RAG". Correction: the first merge missed `f4aee2d` (react and react-dom pinned to 19.3.0), so web tests failed on a react-dom 19.2.8 mismatch. Merged separately; after pulling run `npm ci`. Soham: commit the three missing thread files on the branch if the thread grid should land before 5:00.

- 3:05 UI merge to main: `origin/web-console` (481f355, Soham's distilled console + D3 evidence graph), `origin/zamp-icons` (role gallery at /zamp-roles) and `codex/actual-ui-demo` (/demo rehearsal page) merged; only conflict was this table. The desktop overlay `apps/pet` from `sricharan/work` is in as files only (its channel changes are not), registered as a workspace, Electron pinned 36.9.5. New route `GET /api/pet/findings` in apps/web serves the overlay from the same evidence log as the console (newest first, `revision`, `sample` flag); the overlay defaults to :3100 and takes `--endpoint=<url>`. Verified: overlay launched against a :3130 preview and rendered the live Scenario A card bound to revision …00400. `npm run verify` green (56 + 47 + 35 + 11 + 12). Demo shape: Slack in the browser or desktop, overlay above it in the corner, console as supporting footage. Rev is now 72 px (OpenWhispr-pill size), still by default, red and hopping only while a live finding is unseen; opening the panel stops it. Latest `web-console` (52b42de, typographic pass) merged too.

- 2:08 Someone started `git merge` of the web-console branch (at `1e697be`) inside the main checkout `ThreadRev/` and left it uncommitted; my next commit completed that merge by accident. Undone: main is back on the pushed line, and the merge is preserved as local branch `wip/web-console-merge-2026-09-12` (`ac71aeb`) for whoever owns it. Web lane: merge from your own worktree, never in `ThreadRev/` (AGENTS.md, one checkout per agent). Nothing was lost.

- 1:56 Sixth tool `propose_edit` is live on the channel and in the harness: Rev proposes replacing a printed result with the checker's value, posts Approve/Reject buttons, writes nothing until Approve, then `checkers/apply_docx_edit.py` writes `<doc>-proposed.docx` (git-ignored) with a new sha and the source untouched. Contract change (additive, backend lane): event kinds `edit_proposed`, `edit_decided`, `edit_applied`; graph ignores them, console timeline shows them. Scenario A script now ends with the section 4 proposal (`t = 6.91 s` → `t = 6.493 s`); `evals/records/scripted/scenario-a.1.json` regenerated, `tool_calls` gained one entry, section 4 field names unchanged. `npm run verify` green: 56 + 47 + 34 + 12. Slack lane: A4 step 8 added to the handoff. Demo: shot list step 6 is now the approval beat.

- 1:47 The bot is named Rev (`@Rev` in Slack, Channel code `rev`, display name Rev). The project stays ThreadRev. Every fixture trigger, scripted record, sample log, and doc now says `@Rev` instead of `@reviewer`; prompt identity and welcome header updated. Text only, no numbers or hashes moved. Slack lane: use `--name rev --display-name "Rev"`.

- 1:40 Logo (`assets/threadrev-logo*.svg`), hero image (`assets/hero-scenario-a.png`, web console sample mode: Card 1 STALE, correction, Card 2), and README header are in (`5dbe53d`). Precedents from the three winner repos of the last three months are in `research/presentation-precedents.md`: first prize had logo plus screenshots plus an honesty section, the other two had a tagline and a done-versus-left list, none embedded a video. The accumulated live log `evidence/log.jsonl` (every replay run since morning, 30 findings in one thread) is parked in the session scratchpad so the console renders the clean sample; do not run `npm run replay:record` before the video or it comes back. Video still gated on the Slack lane.

- 1:36 README polish is queued behind a working UI, not started. Gate: the Slack lane reports Card 1 and the stale marker posted live (A4), or the 3:00 PM Path B call. Then, in this order: F14 rerun `npm run verify` and paste real counts into README Status; F15 add a 30-second version under the tagline (recomputes, binds to a revision, goes stale, video link); F12 screenshot of Card 1 with the stale marker under the tagline (Slack card if live, web console if Path B); F13 move the roadmap table and "Why search, not RAG" to `docs/` with one-line links; F16 replace the ASCII diagram with Mermaid; F17 cut the five-part card list in "What it does" to one sentence; F18 fold team rules, repo map, research, sponsors into a collapsed details block so the page ends on Run it and Built versus inherited. Owner: Gabe. Docs only, no verify needed except F14.

- 1:33 Exa is out. It was never registered on the reviewer; the key is blanked in `.env` and every doc that listed it as ours is updated (README status and sponsors, SETUP, SCRATCHPAD, AGENTS, demo and Slack handoffs). Inherited `agent-core` search code and tests untouched. Sponsor line everywhere is OpenAI plus CopilotKit Channels.

- 1:28 README comparison section rewritten in plain terms (Slack keeps the conversation; ThreadRev keeps the decision, what it was decided against, and whether it is still true). `submission/description.md` created with the spoken comparison beat and the written submission description. Demo lane owns wording from here; claims must match the README section.

- 1:25 Differentiation is now written down once, in README "What Slack already does, and what ThreadRev adds": Slack summarizes what was said; ThreadRev keeps what was decided, bound to a revision, numbers recomputed, cards go stale. The video gets one comparison beat right after Card 1 goes stale (exact words in `research/hackathon-design.md` step 5). `AGENTS.md` carries the same framing for agents. Demo lane: the description must match that section.

- 1:20 Slack lane handoff for a computer-use agent is in `research/handoff-slack-lane.md`: A1 CopilotKit login and project select, A2 Channel create and Slack install, A3 model key, A4 live Card 1 and stale-to-Card 2 with screenshots. Report format and stop conditions are in the file. 3:00 PM Path B checkpoint unchanged.

- 12:59 Historical-research demonstration plan and example-customer profile ready in `docs/plans/2026-09-12-1250-feat-historical-research-demo-plan.md`. Aligned with Kestrel Motors reframe; plan-only, no contract or runtime changes. Independent model recovery and live Slack delivery remain separate unproven gates; implementation awaits selection.
- 12:25 Contracts in `contracts/` and `packages/agent-core/src/contracts/` are frozen unless the backend lane announces a change here.
- 12:45 Eval lane split: human owns labels, pass rules, audit, verdict (`evals/labels.md`, `pass-rules.md`, `audit.md`); agent owns cases, grader, matrix (`research/agent-briefs/w5-eval-mechanics.md`). Backend G1 run-record emitter still owed by 1:00 PM; record shape is in the agent brief W5c.
- 1:05 Run record seam frozen: the eleven section 4 field names in `research/handoff-eval-lane.md` are final; `apps/channel/src/replay/record.test.tsx` pins them. Backend lane committed `evals/records/scripted/*.json` per G1; the eval lane owns everything else under `evals/`.
- 1:05 `current_revision` in records is the string `latestRevision()` produced from the transcript: ISO `occurredAt` for a change message, Slack ts for the trigger fallback. Cards bind to the same string, so compare as strings.
- 1:20 If you pulled between `cbf5623` and `9e26775`, `npm run typecheck --workspace agent-core` failed with TS2307 on `./evidence/graph` and `./evidence/log`. Pull `9e26775` or later; nothing else to do.
- 1:35 Preview restarted on :3100 after a low-memory kill; `/api/evidence` 200 (sample). CopilotKit CLI ran `channel:setup` in this checkout: `.gitignore` gained `.env.*` (committed), and `.copilotkit/` is now gitignored because `channels.json` holds the Slack bot token and signing secret. The CLI regenerates it; never force-add it.
- 3:00 PM checkpoint: if `npm run dev:slack` has not reached online, the demo lane records Path B and stops waiting.
- 1:05 Contract change (additive, backend lane): `message_read` events gain optional `channel` and `via: "thread" | "workspace_search"`; new event kind `workspace_search` (query, cutoff, total, returned, hit_ts). Old logs still parse. Graph builder ignores the new kind; hits appear as message nodes through their `message_read` events. Web lane: nothing required, but the console may show `channel` on message nodes found outside the thread.
- 1:05 Fifth reviewer tool `search_workspace` is live on the channel and in the replay harness. Scope decision: the agent must understand the whole workspace, not one thread. Implemented as exact-match search over a structured index (document, unit, quantity words, author, channel, cutoff at the trigger), not embeddings. README section "Why search by identifier, not RAG" is the team's answer to that question.
- 1:30 Demo company reframed: Kestrel Solar Racing (student team) is now Kestrel Motors, an electric vehicle maker. Same channels, cast, timestamps, numbers, checkers, and card arithmetic. Strings only: driver swaps are stops, array input is regen, race director is test lead. The three docx files were regenerated with the new header, so their sha256 values changed (`fixtures/SHA256SUMS`); records, `evidence-log.jsonl`, `graph.test.ts`, and `sample-graph.ts` carry the new hashes. xlsx files and hashes are untouched. `npm run verify` green (56 + 44 + 34 + 10). Commits `e9b5176` and `b1153b5`.
- 1:45 Fixture generator is now byte-for-byte reproducible: openpyxl rewrote `dcterms:modified` on every save, which the zip normalizer did not pin. Fixed in `fixtures/generate.py`, verified by running it twice and diffing the sums. The two xlsx hashes moved once (`fixtures/SHA256SUMS`); four contract examples and the six scripted records carry the new values. Docx hashes unchanged from 1:30. Company framing tightened to "light electric city vehicles" so the 5.2 kWh pack, 120 V bus, and ~300 kg mass read as consistent. `npm run verify` green.
