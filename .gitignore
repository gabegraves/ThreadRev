node_modules/
.env
.env.local
.env.*.local
.copilotkit/artifacts/
*.log
dist/
.DS_Store
.turbo/
*.tsbuildinfo
.next/
next-env.d.ts
assets/fonts/
apps/mobile/.expo/

# Web approval and attempt metadata (not task storage)
.data/

# live evidence log (sample lives in contracts/examples)
/evidence/

# Added by CopilotKit CLI: credential files, which must never be committed.
.env.*
!.env.example

# CopilotKit CLI state: channels.json holds the Slack bot token and signing secret
.copilotkit/

# Rev-applied document edits (live demo output); the fixture sources stay tracked
fixtures/documents/*-proposed.docx
