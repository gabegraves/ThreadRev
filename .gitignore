node_modules/
.env
.env.local
.env.*.local
.copilotkit/artifacts/
*.log
dist/
.DS_Store
.turbo/
*.tsbuildinfo
.next/
next-env.d.ts
assets/fonts/
apps/mobile/.expo/

# Web approval and attempt metadata (not task storage)
.data/

# live evidence log (sample lives in contracts/examples)
/evidence/

# Python bytecode
__pycache__/
*.pyc

# Added by CopilotKit CLI: credential files, which must never be committed.
.env.*
!.env.example

# CLI project selection. No secret in it, but it names one developer's project,
# org and telemetry binding, so committing it drags everyone onto that project.
.copilotkit/
